{
  "name": "commercetools-gcp-extension",
  "version": "1.0.0",
  "description": "Learn how to integrate commercetools API Extensions with Google Cloud Functions",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=applyCartDiscount",
    "deploy": "gcloud functions deploy applyCartDiscount --project PROJECTID --region us-west3 --runtime nodejs12 --trigger-http",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "John Duane",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/functions-framework": "^1.7.1"
  }
}
